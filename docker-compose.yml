services:

  db:
    image: pgrouting/pgrouting:16-3.5-4.0
    environment:
      POSTGRES_DB: dispatchdelivery
      POSTGRES_PASSWORD: secret
    volumes:
      - dispatchdelivery-pg-local:/var/lib/postgresql/data
    ports:
      - "5432:5432"


volumes:
  dispatchdelivery-pg-local: